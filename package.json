{
  "name": "darkget",
  "version": "1.0.0",
  "description": "Script to get data from Dark Sky",
  "main": "index.js",
  "scripts": {
    "debug": "node --nolazy --inspect-brk=5858 -r ts-node/register src/main.ts",
    "start": "rm -rf out && mkdir out && npx ts-node src/main.ts",
    "lint": "npx tslint -c tslint.json 'src/**/*.ts'",
    "build": "npm run lint && npx tsc",
    "clean": "rm -rf node_modules && rm -rf out && mkdir out"
  },
  "author": "James Wood",
  "license": "ISC",
  "dependencies": {
    "@types/node-fetch": "^2.3.3",
    "@types/through2-concurrent": "^2.0.0",
    "csv-parser": "^2.2.0",
    "dot-object": "^1.7.1",
    "dotenv": "^8.0.0",
    "json2csv": "^4.5.1",
    "node-fetch": "^2.5.0",
    "through2-concurrent": "^2.0.0",
    "ts-node": "^8.1.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/json2csv": "^4.4.0",
    "@types/dot-object": "^1.7.0",
    "@types/node": "^12.0.0",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  }
}
